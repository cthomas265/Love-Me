<div class="card">
  <div class="card-header">
    <div class="card-title">
      <h1>{{animals.name}}</h1>
    </div>
    <img width="450px" height="450px" class="rounded-circle" src="{{animals.photo}}" alt="{{animals.description}}" />
  </div>
  <p>{{animals.description}}</p>
</div>