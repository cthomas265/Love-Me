<div class="card-header">
<h1 class="card-title">
    <a href="/animal/{{id}}">{{name}}</a>
</h1>
</div>
<div class="card-body">
<img width="150px" height="250px" class="rounded-circle" src="{{photo}}" alt="{{description}}">
<p>
    {{description}}
</p>
</div>